/* styles.css — page chrome only. Graph styling lives inside the <svg> (render.js)
   so that SVG/PNG export stays self-contained. */

:root {
  --bg: #f1f4f8;
  --surface: #ffffff;
  --line: #dde3ec;
  --ink: #0f172a;
  --ink-soft: #5b6779;
  --ink-faint: #8a95a6;
  --accent: #1d4ed8;
  --danger: #b91c1c;
  --radius: 9px;
  --font: "Segoe UI", Inter, system-ui, -apple-system, sans-serif;
  --mono: ui-monospace, "Cascadia Mono", Consolas, "SF Mono", monospace;
}

* { box-sizing: border-box; }

html, body {
  height: 100%;
  margin: 0;
}

body {
  display: flex;
  flex-direction: column;
  background: var(--bg);
  color: var(--ink);
  font-family: var(--font);
  font-size: 14px;
  overflow: hidden;
}

/* ---------- top bar ---------- */

.topbar {
  flex: none;
  display: grid;
  grid-template-columns: minmax(200px, 1fr) auto;
  grid-template-areas: "titles tools" "legend legend";
  gap: 10px 20px;
  align-items: center;
  padding: 12px 18px 10px;
  background: var(--surface);
  border-bottom: 1px solid var(--line);
  box-shadow: 0 1px 3px rgba(15, 23, 42, .05);
}

.titles { grid-area: titles; min-width: 0; }

.topbar h1 {
  margin: 0;
  font-size: 15px;
  font-weight: 650;
  letter-spacing: -.01em;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.sub {
  margin: 2px 0 0;
  font-size: 11.5px;
  color: var(--ink-faint);
  display: flex;
  gap: 10px;
}

.sub span:not(:empty) + span:not(:empty)::before {
  content: "·";
  margin-right: 10px;
}

.tools {
  grid-area: tools;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px;
}

.group {
  display: flex;
  align-items: center;
  gap: 2px;
  padding: 2px;
  background: #f6f8fb;
  border: 1px solid var(--line);
  border-radius: var(--radius);
}

.tools button {
  font: inherit;
  font-size: 12.5px;
  color: var(--ink-soft);
  background: transparent;
  border: 0;
  border-radius: 6px;
  padding: 5px 10px;
  cursor: pointer;
  white-space: nowrap;
}

.tools button:hover { background: #e6ebf3; color: var(--ink); }

.zoom-label {
  font-size: 11.5px;
  color: var(--ink-faint);
  min-width: 40px;
  text-align: center;
  font-variant-numeric: tabular-nums;
}

.segmented {
  display: flex;
  padding: 2px;
  background: #f6f8fb;
  border: 1px solid var(--line);
  border-radius: var(--radius);
}

.segmented button.is-active {
  background: var(--surface);
  color: var(--ink);
  box-shadow: 0 1px 2px rgba(15, 23, 42, .12);
  font-weight: 600;
}

.search-field { position: relative; display: flex; align-items: center; }

#search {
  font: inherit;
  font-size: 12.5px;
  width: 250px;
  padding: 7px 66px 7px 11px;
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: #f6f8fb;
  color: var(--ink);
}

#search:focus {
  outline: none;
  background: var(--surface);
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(29, 78, 216, .12);
}

.search-status {
  position: absolute;
  right: 10px;
  font-size: 11px;
  color: var(--ink-faint);
  pointer-events: none;
  font-variant-numeric: tabular-nums;
}

.legend {
  grid-area: legend;
  display: flex;
  flex-wrap: wrap;
  gap: 14px;
  font-size: 11px;
  color: var(--ink-faint);
}

.legend:empty { display: none; }

.legend-item { display: inline-flex; align-items: center; gap: 5px; text-transform: uppercase; letter-spacing: .5px; }
.legend-item i { width: 9px; height: 9px; border-radius: 3px; display: inline-block; }

/* ---------- warnings ---------- */

.warnings { display: none; }

.warnings.is-visible {
  display: block;
  position: relative;
  flex: none;
  padding: 9px 40px 9px 18px;
  background: #fff8e6;
  border-bottom: 1px solid #f3dfae;
  color: #7c4a03;
  font-size: 12px;
  max-height: 22vh;
  overflow: auto;
}

.warnings strong { font-weight: 650; }
.warnings ul { margin: 4px 0 0; padding-left: 18px; }
.warnings li { margin: 2px 0; }

.warnings-close {
  position: absolute;
  top: 6px; right: 10px;
  border: 0; background: transparent;
  font-size: 18px; line-height: 1;
  color: #7c4a03; cursor: pointer;
}

/* ---------- workspace ---------- */

.workspace {
  flex: 1;
  display: flex;
  min-height: 0;
}

.stage {
  position: relative;
  flex: 1;
  min-width: 0;
  overflow: hidden;
  cursor: grab;
  background-color: var(--bg);
  background-image: radial-gradient(circle, #d3dae6 1px, transparent 1px);
  background-size: 22px 22px;
}

.stage.is-panning { cursor: grabbing; }
.stage svg { display: block; touch-action: none; }

/* ---------- empty state ---------- */

.empty {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  padding: 24px;
  pointer-events: none;
}

body.has-graph .empty { display: none; }

.empty-card {
  max-width: 560px;
  background: var(--surface);
  border: 1px solid var(--line);
  border-radius: 14px;
  padding: 26px 28px;
  box-shadow: 0 10px 34px rgba(15, 23, 42, .08);
}

.empty-card h2 { margin: 0 0 8px; font-size: 17px; font-weight: 650; }
.empty-card p { margin: 0 0 12px; color: var(--ink-soft); line-height: 1.55; font-size: 13px; }
.empty-card .hint { margin-bottom: 0; color: var(--ink-faint); font-size: 12.5px; }
.empty-card code { font-family: var(--mono); font-size: 12px; background: #eef2f7; padding: 1px 5px; border-radius: 4px; }

.empty-card pre {
  margin: 0 0 14px;
  padding: 12px 14px;
  background: #0f172a;
  color: #d7e3f4;
  border-radius: 9px;
  font-family: var(--mono);
  font-size: 11.5px;
  line-height: 1.6;
  overflow-x: auto;
}

/* ---------- side panel ---------- */

.panel {
  flex: none;
  width: 0;
  overflow: hidden auto;
  background: var(--surface);
  border-left: 1px solid var(--line);
  transition: width .18s ease;
}

.panel.is-open { width: 340px; padding: 20px 22px 32px; position: relative; }

/* drag handle between the stage and the panel */
.panel-resizer {
  flex: none;
  width: 6px;
  cursor: col-resize;
  background: transparent;
  position: relative;
  z-index: 5;
  touch-action: none;
}

.panel-resizer::before {
  content: "";
  position: absolute;
  top: 0; bottom: 0; left: 2px;
  width: 1px;
  background: var(--line);
  transition: background .12s ease, width .12s ease;
}

.panel-resizer:hover::before,
.panel-resizer.is-active::before {
  background: var(--accent);
  width: 2px;
}

.panel.is-resizing { transition: none; }

body.is-resizing-panel { cursor: col-resize; user-select: none; }

.panel-close {
  position: absolute;
  top: 12px; right: 14px;
  border: 0; background: transparent;
  font-size: 22px; line-height: 1;
  color: var(--ink-faint); cursor: pointer;
}

.panel-close:hover { color: var(--ink); }

.panel .kicker {
  margin: 0 0 6px;
  font-size: 10.5px;
  font-weight: 700;
  letter-spacing: .9px;
  text-transform: uppercase;
  color: var(--ink-faint);
}

.panel h2 { margin: 0 0 4px; font-size: 16px; font-weight: 650; line-height: 1.35; padding-right: 22px; }

.panel h3 {
  margin: 20px 0 8px;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: .7px;
  text-transform: uppercase;
  color: var(--ink-faint);
  display: flex;
  align-items: center;
  gap: 7px;
}

.panel .count {
  font-size: 10.5px;
  background: #eef2f7;
  color: var(--ink-soft);
  border-radius: 20px;
  padding: 1px 7px;
  letter-spacing: 0;
}

.mono-id { margin: 0 0 10px; font-family: var(--mono); font-size: 11.5px; color: var(--ink-faint); }
.desc { margin: 10px 0 0; font-size: 13px; line-height: 1.6; color: var(--ink-soft); }
.muted { margin: 0; font-size: 12.5px; color: var(--ink-faint); }

.chips { display: flex; flex-wrap: wrap; gap: 5px; margin-bottom: 4px; }

.chip {
  font-size: 10.5px;
  font-weight: 600;
  letter-spacing: .4px;
  text-transform: uppercase;
  background: #eef2f7;
  color: var(--ink-soft);
  border-radius: 5px;
  padding: 2px 7px;
}

.link-list { list-style: none; margin: 0; padding: 0; }
.link-list li { margin: 3px 0; }

.link {
  font: inherit;
  font-size: 12.5px;
  text-align: left;
  color: var(--accent);
  background: transparent;
  border: 0;
  padding: 3px 0;
  cursor: pointer;
  line-height: 1.45;
}

.link:hover { text-decoration: underline; }

.columns {
  margin: 0;
  padding: 0 0 0 26px;
  font-family: var(--mono);
  font-size: 11.5px;
  color: var(--ink-soft);
  line-height: 1.75;
}

.columns li { padding-left: 2px; }
.columns li::marker { color: var(--ink-faint); font-size: 10px; }

/* sample-data preview table — shown in place of the column list when a
   dataset's "columns" arrived as row objects carrying values. */
.sample-wrap {
  overflow: auto;
  max-height: 320px;
  border: 1px solid var(--line);
  border-radius: 8px;
}

.sample-table {
  border-collapse: collapse;
  font-family: var(--mono);
  font-size: 11.5px;
  white-space: nowrap;
  min-width: 100%;
}

.sample-table th,
.sample-table td {
  padding: 5px 10px;
  text-align: left;
  border-bottom: 1px solid var(--line);
  border-right: 1px solid var(--line);
  max-width: 200px;
  overflow: hidden;
  text-overflow: ellipsis;
}

.sample-table th:last-child,
.sample-table td:last-child { border-right: 0; }

.sample-table thead th {
  position: sticky;
  top: 0;
  background: #f6f8fb;
  color: var(--ink-soft);
  font-weight: 600;
}

.sample-table tbody td { color: var(--ink-soft); }
.sample-table tbody tr:nth-child(even) { background: #fafbfd; }
.sample-table tbody tr:last-child td { border-bottom: 0; }

.sample-more { margin: 6px 0 0; }

/* operation panel: input / output dataset blocks */
.io-block { margin: 0 0 16px; }
.io-block:last-child { margin-bottom: 0; }

.io-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 8px;
  margin: 0 0 6px;
}

.io-name { font-weight: 600; font-size: 13px; padding: 0; }

.io-count {
  flex: none;
  font-size: 10.5px;
  color: var(--ink-faint);
  font-family: var(--mono);
}

.op-title { margin-top: 2px; }
.type-chip { letter-spacing: .5px; }

/* ---------- overlays ---------- */

.drop-overlay {
  position: fixed;
  inset: 0;
  display: none;
  place-items: center;
  background: rgba(15, 23, 42, .55);
  z-index: 40;
}

.drop-overlay.is-open { display: grid; }

.drop-overlay div {
  padding: 26px 44px;
  border: 2px dashed rgba(255, 255, 255, .8);
  border-radius: 14px;
  color: #fff;
  font-size: 17px;
  font-weight: 600;
}

.modal {
  position: fixed;
  inset: 0;
  display: none;
  place-items: center;
  background: rgba(15, 23, 42, .5);
  z-index: 50;
  padding: 24px;
}

.modal.is-open { display: grid; }

.modal-card {
  width: min(680px, 100%);
  background: var(--surface);
  border-radius: 14px;
  padding: 22px;
  box-shadow: 0 20px 50px rgba(15, 23, 42, .3);
}

.modal-card h2 { margin: 0 0 12px; font-size: 16px; font-weight: 650; }

#paste-text {
  width: 100%;
  height: 320px;
  resize: vertical;
  font-family: var(--mono);
  font-size: 12px;
  line-height: 1.55;
  padding: 12px;
  border: 1px solid var(--line);
  border-radius: 9px;
  background: #f8fafc;
  color: var(--ink);
}

#paste-text:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 3px rgba(29, 78, 216, .12); }

.modal-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 14px; }

.modal-actions button {
  font: inherit;
  font-size: 13px;
  padding: 8px 18px;
  border-radius: 8px;
  cursor: pointer;
  border: 1px solid var(--line);
}

.modal-actions .ghost { background: var(--surface); color: var(--ink-soft); }
.modal-actions .ghost:hover { background: #f1f5f9; }
.modal-actions .primary { background: var(--accent); border-color: var(--accent); color: #fff; font-weight: 600; }
.modal-actions .primary:hover { background: #1a43b8; }

.toast {
  position: fixed;
  left: 50%;
  bottom: 26px;
  transform: translate(-50%, 14px);
  max-width: min(560px, calc(100vw - 40px));
  padding: 11px 18px;
  border-radius: 9px;
  background: #0f172a;
  color: #f8fafc;
  font-size: 13px;
  box-shadow: 0 10px 30px rgba(15, 23, 42, .28);
  opacity: 0;
  pointer-events: none;
  transition: opacity .18s ease, transform .18s ease;
  z-index: 60;
}

.toast.is-visible { opacity: 1; transform: translate(-50%, 0); }
.toast.error { background: var(--danger); }
.toast.ok { background: #15803d; }

@media (max-width: 900px) {
  .topbar { grid-template-columns: 1fr; grid-template-areas: "titles" "tools" "legend"; }
  #search { width: 100%; }
  .search-field { flex: 1 1 200px; }
  .panel.is-open { width: 280px; }
}
